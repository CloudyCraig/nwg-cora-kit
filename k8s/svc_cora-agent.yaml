apiVersion: v1
kind: Service
metadata:
  labels:
    app: cora-agent
    part-of: cora-demo
  name: cora-agent
  namespace: natwest
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: cora-agent
  type: ClusterIP
